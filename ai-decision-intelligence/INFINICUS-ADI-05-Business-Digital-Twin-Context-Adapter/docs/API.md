# ADI-05 API

## Block namespace

`window.INFINICUS.ADI.blocks["ADI-05"]`

## Factory

`createDigitalTwinContextAdapter(options)` — returns the ADI-05 engine/service object (`blockId: "ADI-05"`, `version: "1.0.0"`).

## attachToADIRuntime

`attachToADIRuntime(runtime, options)` — validates dependencies, registers the service and routes, returns a result envelope.

## Service id

`adi.digital_twin_context_adapter`

## Routes

| `adi.digital_twin_context.acquire` |
