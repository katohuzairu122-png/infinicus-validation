# Generate Completion Report

Create:

```text
docs/completion-reports/<build-id>-report.md
```

Use the exact report format from the current build prompt.

Include exact files, migration range, test commands, test totals, security verification, known limitations, and next eligible build.

Do not update queue status to `complete` until this report and all required validation exist.
