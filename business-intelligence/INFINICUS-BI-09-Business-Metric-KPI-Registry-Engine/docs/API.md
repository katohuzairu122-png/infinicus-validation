# API

`window.INFINICUS.BI.metricRegistryEngine`

- `registerMetric(input)`
- `publishCalculationHandoff({ metricIds, warehouseSnapshotId, correlationId })`
- `getMetric({ metricId })`
- `getMetricByCode({ code })`
- `getCalculationHandoff({ calculationHandoffId })`
- `listMetrics()`

## Routes

- `bi.metric.register`
- `bi.metric_calculation_handoff.publish`
