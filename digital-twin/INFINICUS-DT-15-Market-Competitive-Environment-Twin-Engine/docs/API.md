# API

`window.INFINICUS.DT.marketCompetitiveTwinEngine`

- `buildMarketCompetitiveState({ marketHandoffId, marketInputs, segmentInputs, competitorInputs, stateInputs, externalForceInputs })`
- `getMarketSnapshot({ marketSnapshotId })`
- `getSyncHandoff({ syncHandoffId })`
- `listTwinMarkets({ twinId })`
- `listTwinCompetitors({ twinId })`
- `listTwinMarketStates({ twinId })`

## Routes

- `dt.market_competitive.build`
