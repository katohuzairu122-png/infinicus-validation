# Integration contract

Attach after ADI-17, ADI-18 and ADI-19. `create` requires their report IDs. An optional `summarizer.summarize(structuredFacts)` may produce a concise summary, but the structured claims remain authoritative.

Output is deliberately labeled `decision_rationale_not_chain_of_thought`.
