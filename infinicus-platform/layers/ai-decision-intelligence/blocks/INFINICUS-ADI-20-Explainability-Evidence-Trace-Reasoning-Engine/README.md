# ADI-20 — Explainability, Evidence Trace and Reasoning Engine

Turns structured analytical outputs into auditable decision rationales, evidence traces, limitations and why-not comparisons. It never exposes private chain-of-thought and does not select or approve an action.

Runtime service: `adi.explainability`. Routes: `adi.explanation.create`, `adi.explanation.get`.
