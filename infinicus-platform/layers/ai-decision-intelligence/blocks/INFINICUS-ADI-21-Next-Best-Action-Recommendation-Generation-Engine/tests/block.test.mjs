import test from"node:test";import assert from"node:assert/strict";import{createRecommendationEngine}from"../src/index.js";const rank={rankingId:"r",tenantId:"t",businessId:"b",decisionId:"d",rows:[{alternativeId:"a",rank:1,eligibilityStatus:"eligible"},{alternativeId:"b",rank:2,eligibilityStatus:"eligible"}]},risk={riskAssessmentId:"k",alternatives:[{alternativeId:"a",criticalRiskIds:[],unquantifiedRiskIds:[]}]},exp={explanationId:"e"};const make=confidence=>createRecommendationEngine({rankingEngine:{get:()=>({ok:true,data:rank})},confidenceEngine:{get:()=>({ok:true,data:{confidenceReportId:"c",alternatives:[{alternativeId:"a",calibratedConfidence:confidence}]}})},riskEngine:{get:()=>({ok:true,data:risk})},explainabilityEngine:{get:()=>({ok:true,data:exp})}});
test("proposes qualified leader without approving action",async()=>{const r=await make(.8).propose({rankingId:"r",confidenceReportId:"c",riskAssessmentId:"k",explanationId:"e",tenantId:"t",businessId:"b"});assert.equal(r.data.recommendedAlternativeId,"a");assert.equal(r.data.status,"proposed");assert.match(r.data.disclaimer,/no business action/i)});
test("withholds recommendation below confidence policy",async()=>{const r=await make(.4).propose({rankingId:"r",confidenceReportId:"c",riskAssessmentId:"k",explanationId:"e",tenantId:"t",businessId:"b"});assert.equal(r.data.recommendedAlternativeId,null);assert.ok(r.data.withholdingReasons.includes("confidence_below_policy"))});
