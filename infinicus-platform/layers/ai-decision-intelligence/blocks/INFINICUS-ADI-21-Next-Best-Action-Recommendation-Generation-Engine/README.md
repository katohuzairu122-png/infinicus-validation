# ADI-21 — Next-Best-Action and Recommendation Generation Engine

Creates a qualified recommendation proposal from rankings, confidence, risks and recorded rationale. It can withhold a recommendation when confidence or risk policy fails. It neither approves nor executes business action.

Service: `adi.recommendation_generation`; routes: `adi.recommendation.propose`, `adi.recommendation.get`.
