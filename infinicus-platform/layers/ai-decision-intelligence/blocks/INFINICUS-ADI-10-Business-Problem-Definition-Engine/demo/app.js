import{createProblemDefinitionEngine}from"../src/index.js";const engine=createProblemDefinitionEngine();document.querySelector("#run").onclick=async()=>{document.querySelector("#output").textContent=JSON.stringify(await engine.create({tenantId:"tenant_demo",businessId:"business_demo",title:"Customer retention is below target",statement:"Monthly customer churn has remained above the approved target for three consecutive periods.",desiredOutcome:"Return monthly churn to the approved target without exceeding the retention budget.",scope:"Customer retention operations",constraints:["Do not exceed approved budget"],successCriteria:["Churn at or below 10%"],urgency:"high"}),null,2)};
