# Integration Contract

The injected `executeScenario` adapter is the only Simulation Engine boundary. It receives an eligible alternative and returns a completed recorded run. ADI-16 copies outputs and builds a comparison matrix; it does not alter models or select an overall winner.
