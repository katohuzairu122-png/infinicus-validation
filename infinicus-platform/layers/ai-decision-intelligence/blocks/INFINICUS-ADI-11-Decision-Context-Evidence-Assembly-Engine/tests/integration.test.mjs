import test from"node:test";import assert from"node:assert/strict";import{attachToADIRuntime}from"../src/index.js";test("registers ADI-11 service and routes",()=>{const services=new Map([["adi.evidence_registry",{}],["adi.goal_registry",{}],["adi.trigger_registry",{}],["adi.problem_definition",{}]]),routes=new Map();const runtime={createId:p=>`${p}_id`,emit:async()=>({ok:true}),success:data=>({ok:true,data}),getService:n=>services.has(n)?{ok:true,data:services.get(n)}:{ok:false},registerService:(n,v)=>{services.set(n,v);return{ok:true}},registerRoute:(n,v)=>{routes.set(n,v);return{ok:true}}};const r=attachToADIRuntime(runtime);assert.equal(r.ok,true);assert.equal(services.has("adi.context_evidence_assembly"),true);assert.equal(routes.size,5)});
