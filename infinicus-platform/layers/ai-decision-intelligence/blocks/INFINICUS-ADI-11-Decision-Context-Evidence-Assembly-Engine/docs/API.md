# Public API

- `assemble({ decisionCase, contextEnvelope, requiredSourceTypes })`
- `get`, `list`, `history`, `verify`
- `assessReadiness(inputs)`
- `attachToADIRuntime(runtime, options)`

Readiness states: `ready`, `needs_data`, `blocked`.
