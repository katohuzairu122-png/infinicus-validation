export function assessReadiness({context,evidence,goals,triggers,problems,verificationFailures,requiredSourceTypes=[]}){const blockers=[],missing=[],warnings=[];if(!problems.length)blockers.push("problem_definition_required");if(!goals.length)missing.push("business_goal_missing");if(!evidence.length)blockers.push("verified_evidence_required");if(verificationFailures.length)blockers.push("evidence_verification_failed");if(context.quality?.usable===false)blockers.push("context_quality_unusable");if(context.conflicts?.length)warnings.push("context_conflicts_present");if(context.providerFailures?.length)warnings.push("context_provider_failures_present");for(const type of requiredSourceTypes){if(!context.fragments.some(x=>x.sourceType===type))missing.push(`source_missing:${type}`)}const status=blockers.length?"blocked":missing.length?"needs_data":"ready";return Object.freeze({status,blockers:Object.freeze(blockers),missing:Object.freeze(missing),warnings:Object.freeze(warnings)})}
