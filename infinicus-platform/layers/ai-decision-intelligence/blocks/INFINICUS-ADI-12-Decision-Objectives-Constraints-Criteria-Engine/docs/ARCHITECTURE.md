# Architecture

ADI-12 creates a versioned DecisionEvaluationFramework from a ready ADI-11 assembly. Objectives define desired decision outcomes, constraints define permitted boundaries, and criteria define later comparison dimensions. Supplied weights are normalized transparently. Validated frameworks are locked before downstream use.
