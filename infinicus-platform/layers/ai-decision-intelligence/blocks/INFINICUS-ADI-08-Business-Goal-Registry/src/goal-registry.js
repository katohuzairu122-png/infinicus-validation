import{GOAL_TYPES,GOAL_PRIORITIES,GOAL_STATUSES,GOAL_DIRECTIONS}from"./constants.js";import{inferDirection,calculateProgress}from"./progress.js";import{createGoalRepository}from"./repository.js";import{success,failure}from"./result-envelope.js";
const localId=prefix=>`${prefix}_${globalThis.crypto?.randomUUID?.()??`${Date.now().toString(36)}_${Math.random().toString(36).slice(2)}`}`;const clean=value=>typeof value==="string"?value.trim():"";

function validate(input,isUpdate=false){const errors=[];if(!clean(input.tenantId))errors.push("tenant_id_required");if(!clean(input.businessId))errors.push("business_id_required");if(!isUpdate&&clean(input.title).length<3)errors.push("title_required");for(const field of["baselineValue","currentValue","targetValue"]){if(!isUpdate&&(!Number.isFinite(Number(input[field]))))errors.push(`${field}_invalid`);if(isUpdate&&input[field]!==undefined&&!Number.isFinite(Number(input[field])))errors.push(`${field}_invalid`)}if(!isUpdate&&!clean(input.unit))errors.push("unit_required");if(input.type&&!GOAL_TYPES.includes(input.type))errors.push("type_invalid");if(input.priority&&!GOAL_PRIORITIES.includes(input.priority))errors.push("priority_invalid");if(input.status&&!GOAL_STATUSES.includes(input.status))errors.push("status_invalid");if(input.direction&&!GOAL_DIRECTIONS.includes(input.direction))errors.push("direction_invalid");if(input.targetDate&&Number.isNaN(Date.parse(input.targetDate)))errors.push("target_date_invalid");return errors}

export function createGoalRegistry(options={}){
 const repository=options.repository??createGoalRepository();const createId=options.createId??localId;const emit=options.emit??(async()=>success());const now=options.now??(()=>new Date());
 async function create(input={}){const errors=validate(input);if(errors.length)return failure("ADI_GOAL_INVALID","Goal failed validation.",{errors});if(input.idempotencyKey){const existingId=repository.idempotentGet({tenantId:input.tenantId,businessId:input.businessId,key:input.idempotencyKey});if(existingId)return get({goalId:existingId,tenantId:input.tenantId,businessId:input.businessId})}const baseline=Number(input.baselineValue),current=Number(input.currentValue),target=Number(input.targetValue),direction=input.direction??inferDirection(baseline,target),createdAt=now().toISOString();const goal=Object.freeze({goalId:input.goalId??createId("goal"),tenantId:input.tenantId,businessId:input.businessId,type:input.type??"growth",title:clean(input.title),description:clean(input.description),baselineValue:baseline,currentValue:current,targetValue:target,unit:clean(input.unit),direction,tolerance:Number(input.tolerance??0),priority:input.priority??"medium",status:input.status??"active",progressPercent:calculateProgress({baselineValue:baseline,currentValue:current,targetValue:target,direction,tolerance:Number(input.tolerance??0)}),targetDate:input.targetDate??null,ownerId:input.ownerId??null,version:1,createdAt,updatedAt:createdAt,schemaVersion:"1.0.0"});if(!repository.create(goal))return failure("ADI_GOAL_DUPLICATE_ID","Goal ID already exists.");repository.idempotentSet({tenantId:goal.tenantId,businessId:goal.businessId,key:input.idempotencyKey,goalId:goal.goalId});await emit("adi.goal.created",goal,{tenantId:goal.tenantId,businessId:goal.businessId});return success(goal)}
 function get(query){const goal=repository.get(query);return goal?success(goal):failure("ADI_GOAL_NOT_FOUND","Goal was not found inside the requested boundary.")}
 function list(query){return success(repository.list(query))}
 function history(query){return success(repository.history(query))}
 async function update(input={}){if(!input.goalId)return failure("ADI_GOAL_ID_REQUIRED","Goal ID is required.");const current=get(input);if(!current.ok)return current;const errors=validate({...input,tenantId:current.data.tenantId,businessId:current.data.businessId},true);if(errors.length)return failure("ADI_GOAL_INVALID","Goal update failed validation.",{errors});const allowed=["type","title","description","currentValue","targetValue","unit","direction","tolerance","priority","status","targetDate","ownerId"];const values={...current.data};for(const field of allowed){if(input[field]!==undefined)values[field]=["baselineValue","currentValue","targetValue","tolerance"].includes(field)?Number(input[field]):input[field]}values.direction=input.direction??inferDirection(values.baselineValue,values.targetValue);values.progressPercent=calculateProgress(values);values.version=current.data.version+1;values.updatedAt=now().toISOString();const next=Object.freeze(values);if(!repository.append(next))return failure("ADI_GOAL_VERSION_CONFLICT","Goal version could not be appended.");await emit("adi.goal.updated",{goalId:next.goalId,version:next.version,status:next.status,progressPercent:next.progressPercent},{tenantId:next.tenantId,businessId:next.businessId});return success(next)}
 async function setStatus(input){return update({...input,status:input.status})}
 async function importLegacy(input={}){return create({...input,tenantId:input.tenantId,type:input.type??"growth",direction:input.direction??inferDirection(Number(input.baselineValue??0),Number(input.targetValue??0)),schemaVersion:undefined})}
 function exportLegacy(query){const found=get(query);if(!found.ok)return found;const goal=found.data;return success({goalId:goal.goalId,businessId:goal.businessId,type:goal.type,title:goal.title,description:goal.description,baselineValue:goal.baselineValue,currentValue:goal.currentValue,targetValue:goal.targetValue,unit:goal.unit,priority:goal.priority,status:goal.status,progressPercent:goal.progressPercent,version:goal.version})}
 return Object.freeze({blockId:"ADI-08",version:"1.0.0",create,get,list,history,update,setStatus,importLegacy,exportLegacy});
}
