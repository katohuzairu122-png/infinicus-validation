# Integration contract

Attach after ADI-20, ADI-21 and ADI-23. `build` accepts an endorsed gate ID. `publish` additionally requires a `publisher.publish(decisionPackage)` adapter and returns its receipt.

The package contract identifies `targetLayer: ABA` and `handoffContract: ABA-02`.
