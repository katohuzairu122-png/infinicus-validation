# Integration Contract

Risk assessors implement `assess({ alternativeId, impacts, run })`. They return risk or opportunity findings with explicit probability sources, impact, evidence, simulation runs and proposed controls. ADI-17 passes the unranked register to ADI-18.
