import test from"node:test";import assert from"node:assert/strict";import{createRedTeamEngine}from"../src/index.js";const rec={recommendationId:"rec",tenantId:"t",businessId:"b",decisionId:"d",rankingId:"r",explanationId:"e",recommendedAlternativeId:"a",status:"proposed"},ranking={rows:[{alternativeId:"a",rank:1,coverage:1,tiedWith:[]}]},explanation={claims:[{claimId:"c"}]};
test("passes a traceable recommendation without mutating it",async()=>{const before=JSON.stringify(rec);const e=createRedTeamEngine({recommendationEngine:{get:()=>({ok:true,data:rec})},rankingEngine:{get:()=>({ok:true,data:ranking})},explainabilityEngine:{get:()=>({ok:true,data:explanation})}});const r=await e.challenge({recommendationId:"rec",tenantId:"t",businessId:"b"});assert.equal(r.data.disposition,"passed");assert.equal(JSON.stringify(rec),before)});
test("records independent blocker",async()=>{const e=createRedTeamEngine({recommendationEngine:{get:()=>({ok:true,data:rec})},rankingEngine:{get:()=>({ok:true,data:ranking})},explainabilityEngine:{get:()=>({ok:true,data:explanation})},challengers:[{challengerId:"c1",challenge:async()=>[{severity:"blocker",code:"COUNTERFACTUAL",message:"Fails counterfactual."}]}]});const r=await e.challenge({recommendationId:"rec",tenantId:"t",businessId:"b"});assert.equal(r.data.disposition,"failed")});
