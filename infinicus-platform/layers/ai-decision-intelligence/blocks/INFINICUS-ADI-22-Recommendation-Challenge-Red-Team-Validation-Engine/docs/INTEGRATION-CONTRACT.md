# Integration contract

Attach after ADI-18, ADI-20 and ADI-21. Optional challengers expose `challenge({ recommendation, ranking, explanation })` and return findings with `severity`, `code`, `message`, and optional evidence refs.
