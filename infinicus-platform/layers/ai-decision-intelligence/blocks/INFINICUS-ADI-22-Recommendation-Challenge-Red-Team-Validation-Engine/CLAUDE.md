# Maintainer guidance

- Preserve the source recommendation byte-for-byte.
- Independent challenge failures are findings, never silently swallowed.
- A blocker produces `failed`; high findings produce `passed_with_conditions`.
- Do not approve, execute or rewrite the recommendation.
