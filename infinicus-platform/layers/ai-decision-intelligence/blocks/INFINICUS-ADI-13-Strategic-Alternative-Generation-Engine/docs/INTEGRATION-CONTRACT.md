# Integration Contract

ADI-13 consumes a locked ADI-12 framework. Generator providers implement `generate(request)` and return proposed alternatives with provenance. Output is an immutable AlternativeSet passed to ADI-14. No scores or recommendations are permitted.
