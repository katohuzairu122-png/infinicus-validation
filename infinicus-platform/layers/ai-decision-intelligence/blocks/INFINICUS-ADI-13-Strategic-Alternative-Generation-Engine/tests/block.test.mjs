import test from"node:test";import assert from"node:assert/strict";import{createAlternativeGenerationEngine}from"../src/index.js";const framework={frameworkId:"fw_1",version:4,status:"locked",tenantId:"t_1",businessId:"b_1",decisionId:"d_1"};const engine=()=>createAlternativeGenerationEngine({frameworkEngine:{get:()=>({ok:true,data:framework})},generators:[{generatorId:"rules",generate:async()=>[{title:"Retention campaign",summary:"Launch a targeted retention programme.",actions:["Segment customers"]},{title:"Retention campaign",summary:"Launch a targeted retention programme."}]}]});
test("generates distinct alternatives with a baseline",async()=>{const r=await engine().generate({frameworkId:"fw_1",tenantId:"t_1",businessId:"b_1"});assert.equal(r.ok,true);assert.equal(r.data.alternatives.length,2);assert.equal(r.data.alternatives[0].actionType,"do_nothing");assert.equal("score"in r.data.alternatives[1],false)});
test("requires a locked framework",async()=>{const e=createAlternativeGenerationEngine({frameworkEngine:{get:()=>({ok:true,data:{...framework,status:"draft"}})}});assert.equal((await e.generate({frameworkId:"fw_1",tenantId:"t_1",businessId:"b_1"})).error.code,"ADI_ALTERNATIVE_FRAMEWORK_UNLOCKED")});
test("requires at least two distinct options",async()=>{const e=createAlternativeGenerationEngine({frameworkEngine:{get:()=>({ok:true,data:framework})}});assert.equal((await e.generate({frameworkId:"fw_1",tenantId:"t_1",businessId:"b_1"})).error.code,"ADI_ALTERNATIVE_DIVERSITY_INSUFFICIENT")});
