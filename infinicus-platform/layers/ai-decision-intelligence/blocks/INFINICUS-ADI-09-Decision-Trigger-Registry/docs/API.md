# Public API

`create`, `get`, `list`, `history`, `update`, `setStatus`, `importLegacy`, `exportLegacy`, and `attachToADIRuntime`.

Supported lifecycle: `open → acknowledged/linked/resolved/dismissed/expired`, with terminal resolved, dismissed and expired states.
