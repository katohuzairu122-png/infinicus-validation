# Integration Contract

1. Attach ADI-01 through ADI-08 first.
2. ADI-09 validates linked goals through ADI-08 when available.
3. ADI-09 registers provider `adi09.trigger_registry` with ADI-04.
4. Detector sources should provide a stable signal fingerprint and evidence IDs.
5. A trigger is a signal only; ADI-10 decides whether it supports a framed problem.
6. Legacy trigger import/export does not edit the original HTML registry.
