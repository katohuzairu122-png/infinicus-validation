import{TRIGGER_TYPES,TRIGGER_SEVERITIES,TRIGGER_STATUSES,TRANSITIONS}from"./constants.js";import{createTriggerRepository}from"./repository.js";import{success,failure}from"./result-envelope.js";
const id=prefix=>`${prefix}_${globalThis.crypto?.randomUUID?.()??`${Date.now().toString(36)}_${Math.random().toString(36).slice(2)}`}`;const text=value=>typeof value==="string"?value.trim():"";
function validate(input,update=false){const errors=[];if(!text(input.tenantId))errors.push("tenant_id_required");if(!text(input.businessId))errors.push("business_id_required");if(!update&&text(input.title).length<3)errors.push("title_required");if(input.triggerType&&!TRIGGER_TYPES.includes(input.triggerType))errors.push("trigger_type_invalid");if(input.severity&&!TRIGGER_SEVERITIES.includes(input.severity))errors.push("severity_invalid");if(input.status&&!TRIGGER_STATUSES.includes(input.status))errors.push("status_invalid");if(input.observedAt&&Number.isNaN(Date.parse(input.observedAt)))errors.push("observed_at_invalid");if(input.expiresAt&&Number.isNaN(Date.parse(input.expiresAt)))errors.push("expires_at_invalid");return errors}

export function createTriggerRegistry(options={}){const repository=options.repository??createTriggerRepository();const createId=options.createId??id;const emit=options.emit??(async()=>success());const now=options.now??(()=>new Date());const resolveGoal=options.resolveGoal;
 async function create(input={}){const errors=validate(input);if(errors.length)return failure("ADI_TRIGGER_INVALID","Trigger failed validation.",{errors});if(input.goalId&&resolveGoal){const goal=await resolveGoal({goalId:input.goalId,tenantId:input.tenantId,businessId:input.businessId});if(!goal?.ok)return failure("ADI_TRIGGER_GOAL_INVALID","Linked goal was not found inside the trigger boundary.")}
  const dedupeKey=input.idempotencyKey??input.signalFingerprint??null;if(dedupeKey){const existingId=repository.dedupeGet({tenantId:input.tenantId,businessId:input.businessId,key:dedupeKey});if(existingId)return get({triggerId:existingId,tenantId:input.tenantId,businessId:input.businessId})}
  const createdAt=now().toISOString();const record=Object.freeze({triggerId:input.triggerId??createId("trigger"),tenantId:input.tenantId,businessId:input.businessId,goalId:input.goalId??null,triggerType:input.triggerType??"manual",title:text(input.title),description:text(input.description),severity:input.severity??"medium",status:input.status??"open",observedAt:input.observedAt??createdAt,expiresAt:input.expiresAt??null,sourceType:input.sourceType??"manual",sourceRef:input.sourceRef??null,evidenceIds:Object.freeze([...(input.evidenceIds??[])]),signalFingerprint:input.signalFingerprint??null,acknowledgedBy:null,acknowledgedAt:null,resolutionReason:null,version:1,createdAt,updatedAt:createdAt,schemaVersion:"1.0.0"});if(!repository.create(record))return failure("ADI_TRIGGER_DUPLICATE_ID","Trigger ID already exists.");repository.dedupeSet({tenantId:record.tenantId,businessId:record.businessId,key:dedupeKey,triggerId:record.triggerId});await emit("adi.trigger.created",record,{tenantId:record.tenantId,businessId:record.businessId});return success(record)}
 function get(query){const record=repository.get(query);return record?success(record):failure("ADI_TRIGGER_NOT_FOUND","Trigger was not found inside the requested boundary.")}
 function list(query){return success(repository.list(query))}function history(query){return success(repository.history(query))}
 async function update(input={}){const found=get(input);if(!found.ok)return found;const errors=validate({...input,tenantId:found.data.tenantId,businessId:found.data.businessId},true);if(errors.length)return failure("ADI_TRIGGER_INVALID","Trigger update failed validation.",{errors});const next={...found.data};for(const field of["title","description","severity","goalId","expiresAt","sourceRef","evidenceIds"]){if(input[field]!==undefined)next[field]=field==="evidenceIds"?Object.freeze([...input[field]]):input[field]}next.version=found.data.version+1;next.updatedAt=now().toISOString();const record=Object.freeze(next);if(!repository.append(record))return failure("ADI_TRIGGER_VERSION_CONFLICT","Trigger version could not be appended.");await emit("adi.trigger.updated",{triggerId:record.triggerId,version:record.version},{tenantId:record.tenantId,businessId:record.businessId});return success(record)}
 async function setStatus(input={}){const found=get(input);if(!found.ok)return found;const status=input.status;if(!TRIGGER_STATUSES.includes(status)||!TRANSITIONS[found.data.status]?.includes(status))return failure("ADI_TRIGGER_TRANSITION_INVALID",`Cannot transition ${found.data.status} to ${status}.`);const next=Object.freeze({...found.data,status,acknowledgedBy:status==="acknowledged"?input.actorId??null:found.data.acknowledgedBy,acknowledgedAt:status==="acknowledged"?now().toISOString():found.data.acknowledgedAt,resolutionReason:["resolved","dismissed"].includes(status)?input.reason??null:found.data.resolutionReason,version:found.data.version+1,updatedAt:now().toISOString()});if(!repository.append(next))return failure("ADI_TRIGGER_VERSION_CONFLICT","Trigger version could not be appended.");await emit("adi.trigger.status.updated",{triggerId:next.triggerId,status:next.status,version:next.version},{tenantId:next.tenantId,businessId:next.businessId});return success(next)}
 async function importLegacy(input){return create({...input,tenantId:input.tenantId,sourceType:input.sourceType??"legacy_goal_registry"})}function exportLegacy(query){const found=get(query);if(!found.ok)return found;const x=found.data;return success({triggerId:x.triggerId,businessId:x.businessId,goalId:x.goalId,triggerType:x.triggerType,title:x.title,description:x.description,severity:x.severity,status:x.status,observedAt:x.observedAt,version:x.version})}
 return Object.freeze({blockId:"ADI-09",version:"1.0.0",create,get,list,history,update,setStatus,importLegacy,exportLegacy})}
