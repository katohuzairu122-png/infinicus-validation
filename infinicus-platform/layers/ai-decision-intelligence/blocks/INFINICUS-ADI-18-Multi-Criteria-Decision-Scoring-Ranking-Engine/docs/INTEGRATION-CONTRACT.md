# Integration Contract

Criterion evaluators return a transparent 0–100 score, evaluator ID, reason and evidence IDs. ADI-18 applies locked normalized weights, preserves missing coverage and ties, and sends the analytical ranking to ADI-19. It makes no recommendation.
