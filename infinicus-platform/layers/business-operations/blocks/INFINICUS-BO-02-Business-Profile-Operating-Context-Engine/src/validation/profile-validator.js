(function(g){"use strict";g.INFINICUS.BO.businessProfileValidator=Object.freeze({validate({profile,policy}){const issues=[];if(policy.status!=="active")issues.push("Policy inactive.");if(policy.requireLegalName&&!profile.legalName)issues.push("Legal name required.");if(policy.requireOperatingCountry&&!(profile.operatingCountries||[]).length)issues.push("Operating country required.");if(policy.requireCurrency&&!(profile.currencies||[]).length)issues.push("Currency required.");if(policy.requireBusinessModel&&!profile.businessModel)issues.push("Business model required.");return{valid:issues.length===0,issues};}});})(window);
