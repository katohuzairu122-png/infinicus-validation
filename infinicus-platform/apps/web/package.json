{
  "name": "@infinicus/web",
  "version": "0.0.1",
  "private": true,
  "description": "INFINICUS public web application",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "dev": "tsc --watch",
    "lint": "eslint src/",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@infinicus/shared-types": "workspace:*",
    "@infinicus/authentication": "workspace:*",
    "@infinicus/configuration": "workspace:*"
  }
}
