# INFINICUS Platform

Monorepo for the INFINICUS business operating and decision-intelligence platform.

## Layer chain

```
DAL → BO → BI → DT → SIM → ADI → ABA → OM → CL → (feedback loop)
```

## Quick start

```bash
pnpm install
pnpm workspace:validate
pnpm build
```

See `CLAUDE.md` for full architecture and contribution rules.
